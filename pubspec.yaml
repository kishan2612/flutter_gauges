name: flutter_gauges
description: Flutter gauges library to create interactive and animated gauges.
version: 1.0.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2

flutter:
